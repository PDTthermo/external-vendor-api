services:
  - type: web
    name: external-vendor-api
    env: node
    plan: free
    buildCommand: |
      npx playwright install --with-deps
    startCommand: node server.js
    envVars: []
