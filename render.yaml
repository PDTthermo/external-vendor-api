services:
  - type: web
    name: external-vendor-api
    env: node
    buildCommand: |
      npm install
      npx playwright install --with-deps
    startCommand: node server.js
    plan: free
    envVars: []
